/*La sintaxis general para declarar variables es la siguiente:

    tipo_var nom_var; //Declaración de una variable
    tipo_var nom_var1, nom_var2; //Declaración de dos variables del mismo tipo
    tipo_var nom_var=valor_inicial; //Declaración e inicialización una variable


*/

/*Ejemplos de declaración de variables

    int f, s=0; //Declara las variables enteras f y s.
                 Además, la variable s la inicializa en 0
    
    float area=0.0, raiz; //Declara las variables reales area y raiz. Ademáz, la variable inicializa en 0.0

    char sexo="f";  //Declara la variable sexo de tipo carácter y la inicializa en "f"

*/

//------------------------------------------------------------
int main()
{
    return 0;
}